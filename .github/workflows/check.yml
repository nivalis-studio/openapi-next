name: Check
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate OpenAPI spec
        run: bun run openapi

      - name: Codegen smoke check
        run: bun run openapi:codegen-smoke

      - name: Check typescript
        run: bun run ts

      - name: Check linter
        run: bun run lint

      - name: Check tests
        run: bun run test

      - name: Check build
        run: bun run build
